version: '3.8'

services:
  scilab-dashboard:
    image: onepersonlab/scilab-agents:latest
    ports:
      - "7891:7891"
    environment:
      - DEMO_MODE=true
    restart: unless-stopped

# Start: docker compose up
# Access: http://localhost:7891
